# Gemini Backend API

This is a simple Express.js backend server that connects to Google's Gemini API using the official SDK.

## Features

- POST endpoint to send a prompt and receive a response from Gemini
- Uses environment variable for API key
- Can be deployed easily on platforms like Render

## Setup

1. Clone the repo:
   ```bash
   git clone https://github.com/your-username/gemini-backend.git
   cd gemini-backend
